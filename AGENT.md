# AI Agent 行为规范

> 本文档定义 AI Agent（Claude、Cursor 等）在「如春日午后阳光」个人站项目中的行为准则。
> 配套：`rules/`（git 工作流、代码质量、文章质量）、`specs/`（文章模板）、`docs/quality-gates.md`（门禁总览）、`.cursor/rules/`、`.cursor/skills/`。

## 核心原则

### 1. 权限边界（最高优先级）

- **只在本仓库内**进行代码与文章创作；一切写操作限于本工作区
- 超出本仓库的操作（写其他工程、系统级改动、发布到外部平台、开通云服务、付费操作）**必须先向用户申请**，获批后再执行
- **部署门禁（即使是"常规"操作）**：`git push` 到 origin、创建/推送 git tag `v*`、`workflow_dispatch` 触发生产部署、删除远端 tag 或 force-push master —— **每一项都必须先得到用户明确同意**，并在执行后向用户汇报 commit SHA / tag / run id。即使用户曾一次性授权过本地 commit 流程，**也不**默认延伸到 push / tag / 部署。
- 用户一次性授权的常规操作（仓库内提交、运行构建/测试、创建与编辑本仓库文件）可酌情自行处理
- 密钥、凭据、`.env` 只读不提交；任何情况下不得把凭据写入代码、文档、日志或对外内容

### 2. 准确性优先

- 技术结论基于代码、文档或实际运行结果，不臆测
- 无法确认的事实（版本行为、外部服务状态）先查证或询问用户
- 引用仓库代码给文件路径；引用外部资料给 URL

### 3. 质量门禁不可绕过

- 提交前：`hooks/pre-commit`（自动）
- 推送前：`hooks/pre-push` 全量门禁（格式 / clippy `-D warnings` / 测试 / wasm 编译 / 文章静态检查），本地手动等价命令 `./tools/test-local.sh`
- 推送后：**盯紧远程 CI**（GitHub Actions，`gh run list` / `gh run watch`），全绿才算完成；失败必须看日志、修根因、重推，不得以本地通过代替远程验证
- **部署**：生产发布走 tag `v*` 推送或 `workflow_dispatch`（需 GitHub Environment `production` 审批），见 [`docs/deploy-vps.md`](docs/deploy-vps.md)；不要绕过该流程手工 rsync 到 VPS
- 本仓库为 **GitHub 开源仓库**：任何提交前确认不含凭据、内网地址、个人信息（pre-commit 会兜底扫描）
- 禁止 `--no-verify` 绕过；禁止无实质理由的 `#[allow]`

### 4. 用户主导

- 重要决策（架构、对外发布、仓库可见性、第三方服务）与用户商议后执行
- 文章发布、仓库公开/迁移等对外动作，必须用户明确确认
- 主动汇报阶段性进展与遇到的问题

## 部署门禁（专门一条，重要）

详细规则见 [`rules/deploy-gating.md`](rules/deploy-gating.md)。**一句话总结**：

> 任何动作会改变 `origin`、生产部署或对外可见状态的，**每一次都问**，不靠"上次允许过"做推断。问完把 commit SHA / tag / run id 回传给用户作为回执。

## 代码工作流程

1. **理解**：先读 `docs/architecture.md`、相关 ADR 与 `.cursor/rules/rcrwhyg.mdc` 硬约束
2. **实现**：遵循 `rules/code-quality.md`；服务器依赖仅挂 `ssr` feature；主题走 `style/tokens.css`
3. **测试**：新逻辑同步补测试（见 `docs/testing.md`）
4. **验证**：`./tools/test-local.sh` 通过
5. **提交**：遵循 `rules/git-workflow.md` 的提交规范；格式修复单独提交
6. **推送与盯守**：推送后确认远程 CI 全绿
7. **汇报**：改动摘要 + 门禁结果

## 文章工作流程（个人网站首发，公众号转载）

1. **选题**：与用户确认主题；素材优先来自本项目真实实践
2. **撰写**：复制 `articles/templates/standard-template.md`，遵循 `specs/article-template.md`
3. **自检**：`./tools/check-articles.sh` 通过；代码示例实际验证过
4. **提交**：`content:` 前缀提交；状态更新到 `articles/README.md`
5. **审核**：提交用户审核，意见记入 `docs/article-reviews.md`，迭代到通过
6. **发布**：用户确认内容为最终版后，由用户自行决定是否转载到微信公众号【如春日午后阳光】（使用其指定的公众号排版工具，可加"阅读原文"指向个人网站 permalink）；AI 不发布任何内容到外部平台

## 内容与对外安全

- 文章与公开内容不得泄露：服务器地址、口令、`.env` 内容、私有仓库路径、未公开计划
- 截图/日志素材先检查是否含凭据
- 涉及真人（用户本人或他人）的信息，发布前需用户确认

## 交互规范

- 完成阶段性工作主动汇报（改了什么 / 门禁结果 / 遗留问题）
- 遇到被权限边界挡住的需求，明确说明需要什么授权
- 建议要给出理由与代价，供用户选择，不越权代决

## 紧急情况

- **凭据泄露**：立即告知用户，轮换凭据优先于清理历史
- **门禁与紧急修复冲突**：可以 `--no-verify` 推送热修复，但同一会话内必须补齐门禁并记录
- **CI 持续失败**：保留现场（分支 + 日志链接），汇报用户，不带病合并

---
*本规范随项目演进更新；修订需提交 `docs:` 说明。*
